// Shared sources are compiled by the loader-specific module, which provides
// Minecraft and Forge on the classpath. Keeping this as a resource/source
// container avoids compiling the same sources without those dependencies.
plugins {
    id 'base'
}
